/* eslint-disable @typescript-eslint/explicit-function-return-type */
/* eslint-disable @typescript-eslint/restrict-plus-operands */
/* eslint-disable id-length */

const flattenedPass = [].flat();

const otherReduce = [].reduce((p, n) => n + p, 0);
