/* eslint-disable @typescript-eslint/strict-boolean-expressions */
/* eslint-disable no-empty */

/*
 * Currently no non-redundant checks for this rule are defined, therefore it
 * is tested if alternatives still catch the invalid behavior.
 */
if (Promise.resolve()) {
}
